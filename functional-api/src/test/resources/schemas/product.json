{
  "id": "#? typeof _ === 'string' && _.trim().length > 0",
  "name": "#? typeof _ === 'string' && _.trim().length > 0",
  "description": "#string",
  "price": "#? typeof _ === 'number' && _ >= 0",
  "is_location_offer": "#boolean",
  "is_rental": "#boolean",
  "in_stock": "#? _ === true || _ === false || typeof _ === 'number'",
  "co2_rating": "#string",
  "is_eco_friendly": "#boolean",
  "brand": "##object",
  "category": "##object",
  "product_image": "##object"
}
